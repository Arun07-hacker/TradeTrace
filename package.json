{
  "name": "tradetrace-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "TradeTrace — AI Trading Research, Risk & Learning Assistant",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run start",
    "lint": "npm --prefix frontend run lint",
    "test:backend": "cd backend && .\\venv\\Scripts\\pytest -v",
    "dev:backend": "cd backend && .\\venv\\Scripts\\uvicorn app.main:app --reload --port 8000"
  }
}
